{% extends 'base.html.twig' %}

{% block title %}IssueComment{% endblock %}

{% block body %}
    <h1>IssueComment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ issue_comment.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ issue_comment.content }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ issue_comment.updatedAt ? issue_comment.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('issue_comment_index') }}">back to list</a>

    <a href="{{ path('issue_comment_edit', {'id': issue_comment.id}) }}">edit</a>

    {{ include('issue_comment/_delete_form.html.twig') }}
{% endblock %}
