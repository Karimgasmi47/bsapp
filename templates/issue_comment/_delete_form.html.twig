<form method="post" action="{{ path('issue_comment_delete', {'id': issue_comment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ issue_comment.id) }}">
    <button class="btn">Delete</button>
</form>
